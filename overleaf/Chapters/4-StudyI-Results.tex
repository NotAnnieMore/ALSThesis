\chapter{Functional Progression Prediction: Results and Discussion}
\label{ch:study1_results}

% ============================================================
\section{Cohort and Target Feasibility}
\label{sec:results_cohort}
% ============================================================

\subsection{Eligibility by Horizon}
\label{subsec:results_eligibility}
Starting from $N\!=\!5{,}436$ subjects with at least one ALSFRS-R record in the PRO-ACT database, the effective cohort size is determined by the availability of follow-up measurements within each target-horizon window (Table~\ref{tab:eligibility}).

For the \textbf{6-month horizon} ($H\!=\!180 \pm 7$\,d), the final dataset contains $N\!=\!1{,}392$ subjects (25.6\% of the initial pool), partitioned into DEV ($n\!=\!1{,}113$, 80\%) and TEST ($n\!=\!279$, 20\%).
For the \textbf{3-month horizon} ($H\!=\!90 \pm 7$\,d), $N\!=\!2{,}407$ subjects (44.3\%) are eligible, split into DEV ($n\!=\!1{,}925$) and TEST ($n\!=\!482$).
A total of 720 subjects are eligible at both horizons.
The substantial attrition from 5{,}436 to 1{,}392 at 6 months reflects two structural properties of the PRO-ACT database:
(i)~many clinical trials had follow-up periods shorter than 180~days, so subjects were never assessed near the 6-month mark;
and (ii)~ALS patients who deteriorated rapidly may have dropped out, been transferred to palliative care, or died before the 180-day window, creating informative censoring that disproportionately removes fast progressors.
Consequently, the 6-month cohort is likely enriched for slower-progressing subjects relative to the full PRO-ACT population, a caveat that should be borne in mind when interpreting the results.
Nevertheless, the 6-month horizon provides greater temporal support for slope estimation (99.8\% based on $\geq 3$ data points, median 7) than the 3-month horizon.

\begin{table}[ht!]
\centering
\caption[Cohort size by prediction horizon]{Cohort size and data partition by prediction horizon.}
\label{tab:eligibility}
\small
\begin{tabular}{lccccc}
\toprule
Horizon & $N$ & \% of PRO-ACT & DEV & TEST & Both horizons \\
\midrule
3 months & 2{,}407 & 44.3\% & 1{,}925 & 482 & \multirow{2}{*}{720} \\
6 months & 1{,}392 & 25.6\% & 1{,}113 & 279 & \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Target Definition and Slope Distribution}
\label{subsec:results_target_summary}
For each subject, functional progression is quantified as the ALSFRS-R slope between baseline and the target horizon, expressed in points per 30~days (see Section~\ref{sec:methods_slope}).
Negative slopes indicate functional decline; the more negative, the faster the deterioration.

The distribution of slopes is left-skewed for both horizons:
\begin{itemize}
  \item \textbf{6-month:} mean $= -0.95 \pm 0.89$\,pts/30d, median $= -0.80$, range $[-5.23,\; +0.85]$.
  \item \textbf{3-month:} mean $= -0.90 \pm 1.16$\,pts/30d, median $= -0.68$, range $[-7.79,\; +2.16]$.
\end{itemize}
The 3-month slopes exhibit higher variance ($\sigma = 1.16$ vs $0.89$), consistent with fewer data points per slope estimate and wider measurement noise over a shorter observation window.
Despite this, 90.5\% of 3-month slopes are based on $\geq 3$ ALSFRS-R measurements (median $n_{\text{points}} = 4$), and 99.8\% of 6-month slopes use $\geq 3$ points (median $n_{\text{points}} = 7$), confirming adequate estimation quality for both horizons.

The binary target (\textit{rapid} vs \textit{slow}) is derived \textbf{fold-wise} during cross-validation by labelling the worst 30\% slopes in the \emph{training} fold as \textit{rapid}.
For the 6-month horizon, the global 30th percentile corresponds to a slope of $-1.25$\,pts/30d; in practice, the fold-wise cutoffs range from $-1.27$ to $-1.19$ (mean $-1.24$), identifying subjects who are declining by more than approximately one ALSFRS-R point per month.

\subsection{Feature Coverage}
\label{subsec:results_coverage}
Feature availability varies substantially across blocks (Table~\ref{tab:feature_coverage}).
The baseline block has effectively complete coverage: ALSFRS-R total and sex are available for 100\% of subjects, while age is present for 87.1\% ($n\!=\!1{,}212$).
The 180 subjects without recorded age correspond to clinical trials in which age was not collected or was anonymised; because this missingness is administrative rather than outcome-dependent, median imputation is applied without risk of introducing prognostic bias.
Vital signs are available for 80.6\% of subjects at a block level ($n\!=\!1{,}122$), with individual features ranging from 44.9\% (height) to 79.2\% (weight).
FVC data are available for 56.3\% (litres) to 54.5\% (percent of normal), motivating the missingness analysis in Section~\ref{sec:results_fvc_missingness}.
At block level, 60.7\% of subjects ($n\!=\!845$) have at least one non-missing FVC feature.
Study arm is recorded for 91.1\% of subjects. The derived riluzole indicator and concomitant-medication count contain no missing values because subjects without a qualifying pre-baseline record are coded as zero.
Labs are moderately available (creatinine: 64.7\%, ALT: 69.0\%).

\begin{table}[ht]
\centering
\caption[Feature coverage, 6-month cohort]{Feature coverage for the 6-month cohort ($N\!=\!1{,}392$). Block-level coverage indicates the fraction of subjects with at least one non-missing feature in the block.}
\label{tab:feature_coverage}
\small
\begin{tabular}{lcc}
\toprule
Feature Block & Features ($n$) & Coverage (\%) \\
\midrule
Baseline (demographics + ALSFRS-R) & 15 & $\sim$100 \\
Vitals & 12 & 80.6 \\
Respiratory (FVC) & 3 & 60.7 \\
Treatment & 3 & $\sim$100 \\
Labs & 2 & 64.7--69.0 \\
\midrule
All features & 35 & 100\textsuperscript{$\dagger$} \\
\bottomrule
\multicolumn{3}{l}{\footnotesize $\dagger$After median imputation, all subjects have complete feature vectors.}
\end{tabular}
\end{table}

The heterogeneous coverage across blocks underscores the importance of an imputation strategy that does not introduce outcome-dependent bias, and supports the use of ablation to quantify whether blocks with lower coverage (e.g.\ FVC, Labs) contribute sufficient predictive signal to justify their inclusion.
Note that the dataset contains 39 columns in total, but 4 columns are excluded before modelling: three temporal offsets (\texttt{t0\_delta\_days}, \texttt{vitals\_delta\_days}, \texttt{fvc\_delta\_days}) that encode measurement timing rather than clinical status, and \texttt{ALSFRS\_Responded\_By} which is near-constant ($>$93\% missing).
The remaining 35 features constitute the model input after imputation.


% ============================================================
\section{Data Quality: FVC Missingness Analysis}
\label{sec:results_fvc_missingness}
% ============================================================

FVC is available for approximately 56\% of the 6-month cohort.
Because missing data can introduce bias if its absence is related to the outcome (Missing Not At Random, MNAR), it is essential to test whether patients with and without FVC differ systematically on key clinical variables.
We compared the two groups across 11 variables using Mann--Whitney~U tests for continuous variables and $\chi^2$ tests for categorical variables, with effect sizes reported as Cohen's~$d$ and Cramér's~$V$ respectively (Table~\ref{tab:fvc_missingness}).

\input{images/tables/step5v2_fvc_missingness.tex}
\break



\noindent\textbf{Key findings.}
Of the 11 tests performed, we first note that the three variables most directly related to the prediction task show no significant association with FVC availability:
ALSFRS-R at baseline ($p\!=\!0.345$, $d\!=\!0.027$), age ($p\!=\!0.459$, $d\!=\!0.031$), and sex ($p\!=\!0.408$, $V\!=\!0.022$).
The target slope approaches borderline significance ($p\!=\!0.050$, $d\!=\!0.098$); however, the effect size is trivial ($d < 0.10$), and the $p$-value does not survive correction for multiple comparisons (Bonferroni-adjusted $\alpha\!=\!0.05/11\!=\!0.0045$).
In substantive terms, mean slopes are $-0.91$ (with FVC) vs $-1.00$ (without FVC)~pts/30d---a difference of $0.09$~pts/30d that is clinically negligible relative to the overall slope standard deviation ($\sigma\!=\!0.89$).
Similarly, lab values (creatinine $p\!=\!0.092$, ALT $p\!=\!0.097$) show no significant difference.

The clearest differences involve study arm ($V\!=\!0.357$, $p < 0.001$) and number of concomitant medications ($d\!=\!0.332$, $p < 0.001$). These associations are compatible with variation in measurement protocols across the pooled trials, although the available variables do not establish the reason for the missing records.
Riluzole shows a nominally significant association ($p\!=\!0.015$, $V\!=\!0.065$), but the effect is negligible and does not survive Bonferroni correction ($p > 0.0045$).
Weight ($d\!=\!0.146$, $p\!=\!0.003$) and BMI ($d\!=\!0.468$, $p < 0.001$) also differ between groups. Their availability may be correlated with FVC collection, but this explanation was not tested directly.

FVC availability shows no clear association with baseline ALSFRS-R, age, sex, or the six-month slope after accounting for effect size and multiple comparisons. Together with the association observed for study arm, this pattern is compatible with missingness related partly to trial-level collection practices. It does not prove that the data are \emph{Missing at Random}, and an \gls{MNAR} component cannot be excluded from the available observations.
Median imputation is therefore retained as a pragmatic preprocessing choice rather than as a consequence of a confirmed missing-data mechanism. The Bonferroni threshold ($\alpha/11\!=\!0.0045$) leaves study arm, BMI, weight, and concomitant-medication count as the clearest observed differences between the groups.


% ============================================================
\section{Data Quality: BMI Correction}
\label{sec:results_bmi_fix}
% ============================================================

During exploratory analysis of the vital signs table, we identified a systematic data-quality issue affecting \gls{BMI} computation.
A total of 147 height records in the raw PRO-ACT data were labelled ``Centimeters'' but contained numeric values in the range 14.5--19.3, far below any plausible adult stature.
Multiplied by ten, these values yield 145--193\,cm, which falls within a normal adult range; the most likely explanation is therefore a decimal-point shift during data entry (e.g.\ 173\,cm recorded as 17.3).
Because the feature-extraction function \texttt{to\_cm} passes ``Centimeters''-labelled values through unchanged, these erroneous heights propagated into the baseline feature file.
When used directly in the \gls{BMI} formula ($\text{BMI} = \text{weight\,(kg)} \;/\; [\text{height\,(m)}]^{2}$), a height of 14.5\,cm paired with a typical weight of ${\sim}$95\,kg produces an impossible \gls{BMI} of ${\approx}$4{,}500, confirming the records as data-quality artefacts.

After selecting the measurement closest to $t_0$ per subject, 132 of the original 147 raw records remained in the baseline feature file (\texttt{features\_vitals\_t0.csv}, $N = 4{,}438$).
We corrected the issue by applying plausible adult bounds---Height $\in [120, 220]$\,cm; Weight $\in [30, 200]$\,kg---and setting any value outside these ranges to \texttt{NaN}.
This affected 132 height entries and 31 weight entries; the resulting missing values are handled downstream by median imputation inside each model's preprocessing pipeline (see Section~\ref{sec:results_optuna_v2}).
After correction, \gls{BMI} in the six-month analysis cohort ($N = 1{,}392$) ranges from 15.5 to 46.9, consistent with published \gls{BMI} distributions for \gls{ALS} patients in clinical trials~\cite{Anani2023}.
All datasets (used in tuning, ablation, and all subsequent analyses) were regenerated from the corrected vitals file to ensure that no downstream result is affected by the original erroneous values.


% ============================================================
\section{Exploratory Analysis: Treatment vs Disease Progression}
\label{sec:results_eda_treatment}
% ============================================================

Three treatment-related features are available in the dataset: clinical trial arm (\texttt{study\_arm}), pre-baseline riluzole use (\texttt{riluzole\_pre\_t0}), and the number of concomitant medications (\texttt{n\_conmeds\_pre\_t0}).
Their distributions are examined against the six-month \gls{ALSFRS-R} slope and a descriptive rapid-progression label based on the global 30th percentile (418 of 1,392 subjects, 30.0\%). This global label is used only in this exploratory section and not for model validation.

\textbf{Important caveat.}
This analysis is observational and does not estimate treatment effects. PRO-ACT aggregates multiple trials, so ``Active'' denotes exposure to different experimental interventions rather than a single treatment. Riluzole use and concomitant medication burden may also be associated with patient characteristics, trial procedures, and recording practices. The comparisons below are therefore descriptive.

\subsection{Study Arm: Active vs Placebo}
\label{subsec:results_eda_arm}

Of the 1{,}392 subjects in the 6-month cohort, 506 belong to the Active arm, 762 to Placebo, and 124 have no arm information.
The observed rapid-progression rates are 26.5\% in Active, 31.6\% in Placebo, and 34.7\% among subjects with missing arm information (Table~\ref{tab:eda_treat_arm}).
Median slopes are similar: $-0.791$ (Active) vs $-0.819$ (Placebo) points/30\,d.

\begin{table}[ht!]
\centering
\caption[Descriptive statistics by study arm]{Descriptive statistics by study arm (6-month cohort).}
\label{tab:eda_treat_arm}
\small
\begin{tabular}{lrccc}
\toprule
Group & $N$ & Rapid (\%) & Slope mean & Slope median \\
\midrule
Active   & 506 & 26.5 & $-0.901$ & $-0.791$ \\
Placebo  & 762 & 31.6 & $-0.973$ & $-0.819$ \\
Missing  & 124 & 34.7 & $-1.022$ & $-0.689$ \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:eda_arm_dist} shows the slope distributions and boxplots by arm, revealing largely overlapping distributions.
Figure~\ref{fig:eda_arm_rapid} displays the rapid-progression rates with numeric annotations.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/eda_treat_arm_slope_dist.png}
  \caption[ALSFRS-R slope distribution by study arm]{ALSFRS-R slope distribution by study arm: histogram (left) and boxplot (right). The dashed red line marks the 30th-percentile threshold ($P_{30} = -1.25$).}
  \label{fig:eda_arm_dist}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.55\linewidth]{images/figures/eda_treat_arm_rapid_rate.png}
  \caption[Rapid progression rate by study arm]{Rapid-progression rate by study arm. The dashed line indicates the global prevalence (30\%).}
  \label{fig:eda_arm_rapid}
\end{figure}

\clearpage
A $\chi^2$ test of independence between study arm and rapid-progression status, restricted to the 1,268 subjects with known arm information, yields $\chi^2 = 3.62$, $p = 0.057$.
A Mann-Whitney~$U$ test comparing the slope distributions of Active vs Placebo gives $U = 200{,}472$, $p = 0.229$, confirming no statistically significant difference.
The observed difference in rapid-progression rates does not meet the predefined significance level, and the pooled arm labels do not support treatment-specific interpretation.

\subsection{Riluzole}
\label{subsec:results_eda_riluzole}

Documented pre-baseline riluzole use is present for 902 subjects (64.8\%); the remaining 490 have no qualifying use record. Rapid-progression rates are 30.8\% and 28.6\%, respectively. Median slopes are also close ($-0.807$ vs $-0.777$ points/30\,d). These comparisons describe the coded exposure groups and should not be read as an estimate of riluzole efficacy~\cite{Miller2012}.
Figure~\ref{fig:eda_riluzole} shows both distributions.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/eda_treat_riluzole_slope_dist.png}
  \caption[Slope distribution and progression rate by riluzole use]{Slope distribution (left) and rapid-progression rate (right) by riluzole use. Median slopes are annotated on each bar.}
  \label{fig:eda_riluzole}
\end{figure}

The binary label is not associated with documented riluzole use ($\chi^2 = 0.66$, $p = 0.416$), and the continuous slope comparison gives the same conclusion (Mann--Whitney $U = 216{,}535$, $p = 0.534$).

\subsection{Interaction: Study Arm $\times$ Riluzole}
\label{subsec:results_eda_interaction}

The cross-tabulation in Figure~\ref{fig:eda_interaction} shows rates ranging from 25.7\% in Active subjects with documented riluzole use ($n\!=\!315$) to 33.5\% in the corresponding Placebo subgroup ($n\!=\!526$). The two groups without documented use have similar rates: 27.7\% for Active ($n\!=\!191$) and 27.5\% for Placebo ($n\!=\!236$). No formal interaction test was performed, so these differences are presented without an interaction claim.\\\\

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.55\linewidth]{images/figures/eda_treat_interaction.png}
  \caption[Rapid progression rate by study arm and riluzole]{Rapid-progression rate by study arm $\times$ riluzole subgroup. The dashed line indicates the global prevalence (30\%).}
  \label{fig:eda_interaction}
\end{figure}

The subgroup pattern may reflect differences between trials or patient groups that are not represented in this cross-tabulation. It does not establish that either riluzole or assignment to an experimental arm altered functional progression.

\subsection{Concomitant Medications and Top Drugs}
\label{subsec:results_eda_conmeds}

The number of pre-baseline concomitant medications (\texttt{n\_conmeds\_pre\_t0}) shows a non-linear relationship with rapid-progression rate: patients with 1--2 medications have the lowest rate ($22.8$\%, $n\!=\!149$), while those with 6--9 medications have the highest ($34.2$\%, $n\!=\!310$).
Medication count may capture several factors, including comorbidity, symptom management, trial procedures, and record density; the grouped rates do not separate these explanations.

The 15 most frequently recorded medications are then identified from \texttt{PROACT\_CONMEDS} using subject-specific baseline dates (Figure~\ref{fig:eda_top_meds}). Riluzole is the most common entry in this table (673 subjects, 48.3\%), followed by Vitamin~E (355, 25.5\%) and ascorbic acid (311, 22.3\%). The rapid-progression rates range from 25.4\% for Betacarotene to 37.3\% for Colecalciferol. These are unadjusted descriptive rates; no medication-specific hypothesis tests are performed, and the raw concomitant-medication entry for riluzole is distinct from the dedicated riluzole-use feature described above.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/eda_treat_top_meds.png}
  \caption[Top 15 pre-baseline medications by prevalence]{Top 15 pre-baseline medications by cohort prevalence, coloured by whether their rapid-progression rate exceeds the global prevalence (30\%, red dashed line). Green bars fall below the global rate.}
  \label{fig:eda_top_meds}
\end{figure}

\clearpage
\subsection{Summary}
\label{subsec:results_eda_treat_summary}

Neither study arm nor documented riluzole use is significantly associated with the descriptive rapid-progression label at $\alpha=0.05$. Their continuous slope distributions also overlap. Medication count and individual medication rates show variation, but these comparisons are unadjusted and were not designed to identify pharmacological effects.

The three treatment features are retained as candidate predictors of patient and trial heterogeneity. Their incremental predictive contribution is assessed through ablation rather than inferred from this \gls{EDA}, and subsequent coefficients or \gls{SHAP} values are interpreted as model associations rather than causal effects.


% ============================================================
\section{Unified Optuna Hyperparameter Tuning (6-Month Horizon)}
\label{sec:results_optuna_v2}
% ============================================================

Seven model families were tuned with Optuna (60 trials per configuration, \gls{TPE} sampler, seed~42) using subject-wise GroupKFold ($K\!=\!5$), fold-wise binary label definition, and the \gls{DEV} partition only ($n\!=\!1{,}113$).
The seven families---Logistic Regression, Decision Tree, \gls{KNN}, \gls{SVM}, Random Forest, \gls{LGBM}, and \gls{XGB}---were chosen to span linear, distance-based, kernel-based, single-tree, and ensemble paradigms, allowing a systematic comparison across model complexity levels.
Each model was evaluated in both \textbf{unbalanced} and \textbf{balanced} modes---except \gls{KNN}, which lacks native class-weight support, yielding 13 configurations in total.
The objective metric was \gls{PR-AUC} (average precision). After each search, \gls{ROC-AUC} and threshold-dependent metrics ($F_1$, $F_2$ at threshold~0.5) were collected for the selected configuration.
Sixty trials were used as a common computational budget rather than as a formal convergence criterion. All 780 trials completed successfully. Nine of the 13 searches found their final best value between trials 41 and 60, including both XGBoost modes, so the histories do not support truncating the budget at 40 trials. The searched spaces ranged from two parameters for Logistic Regression to nine for LightGBM and XGBoost, although the sampled LightGBM \texttt{subsample} parameter was inactive under the default \texttt{subsample\_freq=0}.
The full tuning required approximately 34 minutes of recorded optimisation time on a single machine, with per-configuration times ranging from 8\,s (Decision Tree) to 768\,s (\gls{SVM} unbalanced).

Table~\ref{tab:step5v2_tuning} and Figure~\ref{fig:prauc_barchart} summarise the results.

\input{images/tables/step5v2_tuning_comparison.tex}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/step5v2_prauc_barchart.png}
  \caption[Cross-validation PR-AUC by model, 6-month]{PR-AUC comparison of Optuna-tuned models (6-month, DEV). Blue bars: unbalanced mode; orange bars: balanced mode. Error bars show $\pm 1$ standard deviation across 5 folds.}
  \label{fig:prauc_barchart}
\end{figure}

\clearpage
\noindent\textbf{Key observations.}
\begin{enumerate}
  \item \textbf{Top-ranked model.} XGBoost (unbalanced) achieves the highest PR-AUC ($0.434 \pm 0.049$), closely followed by Random Forest (unbalanced, $0.431 \pm 0.042$) and SVM (balanced, $0.425 \pm 0.049$).
    The small differences among the leading configurations show that several model families achieve similar internal validation performance; they do not, by themselves, establish a performance ceiling.
  \item \textbf{Effect of class balancing on PR-AUC.} Weighting produces small changes for Random Forest ($0.431 \to 0.425$), LightGBM ($0.415 \to 0.418$), and XGBoost ($0.434 \to 0.424$). The largest change occurs for SVM, whose PR-AUC increases from $0.389$ to $0.425$. The effect is therefore model-dependent rather than uniformly beneficial or detrimental.
  \item \textbf{Threshold-dependent metrics.} At the default threshold of 0.5, $F_1$ and $F_2$ are low for most unbalanced models, although Decision Tree is a clear exception. Weighting substantially increases these scores for Decision Tree, Random Forest, Logistic Regression, LightGBM, and XGBoost; for example, Logistic Regression $F_2$ increases from $0.028$ to $0.527$. Balanced SVM remains near zero at this cutoff despite its improved PR-AUC. These results show that performance at a fixed threshold depends on the probability scale as well as on ranking ability (Section~\ref{sec:results_threshold_xgb}).
  \item \textbf{Interpretable baselines.} Logistic Regression balanced ($\text{PR-AUC} = 0.407$) performs only $0.027$ below XGBoost unbalanced, making it a competitive interpretable alternative.
    Its coefficients can be directly analysed to understand feature directionality.
\end{enumerate}


% ────────────────────────────────────────────────────────────
\subsection{Balanced vs Unbalanced: Paired Comparison}
\label{subsec:results_bal_vs_unbal}
% ────────────────────────────────────────────────────────────

Figure~\ref{fig:bal_vs_unbal} presents a paired (dumbbell) comparison of PR-AUC and ROC-AUC for each model family across the two class-weighting modes.
The differences between modes are below 0.01 PR-AUC for Random Forest, LightGBM, and XGBoost. Decision Tree improves by 0.016 with weighting, while Logistic Regression changes by 0.006. SVM shows the largest difference, increasing by 0.036 in balanced mode. Thus, class weighting has little effect on ranking performance for most model families in this experiment, but the SVM result shows that this cannot be assumed for every classifier.

The much larger differences in $F_1$ and $F_2$ at threshold 0.5 reflect changes in the scale and distribution of predicted probabilities. They should not be interpreted as equivalent changes in threshold-free discrimination. This distinction motivates selecting the model by PR-AUC and choosing its operating threshold separately on development data.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/step5v2_balanced_vs_unbalanced.png}
  \caption[Balanced vs.\ unbalanced mode comparison]{Paired comparison of unbalanced ($\circ$) vs balanced ($\square$) modes for each model. Left panel: PR-AUC; right panel: ROC-AUC. Lines connect the same model across both modes.}
  \label{fig:bal_vs_unbal}
\end{figure}


% ────────────────────────────────────────────────────────────
\subsection{Metric Rank Correlation}
\label{subsec:results_metric_correlation}
% ────────────────────────────────────────────────────────────

We computed Spearman rank correlations between all pairs of cross-validated evaluation metrics across the 13 model $\times$ mode configurations to compare the resulting model rankings.
If two metrics produce highly correlated model rankings, they provide redundant information; if uncorrelated, they capture different aspects of model behaviour.

The correlations quantify agreement between rankings but do not make the metrics interchangeable, since each captures a different aspect of performance. Two patterns are visible:
\begin{itemize}
  \item \textbf{Threshold-free metrics:} PR-AUC and ROC-AUC are strongly correlated ($\rho\!=\!0.758$, $p\!=\!0.003$), indicating that models which rank probability predictions well under one curve generally rank well under the other.
    The two measures therefore produce broadly similar, although not identical, model rankings.
  \item \textbf{Threshold-dependent metrics at fixed cutoff:} $F_1$ and $F_2$ at threshold~0.5 are almost perfectly correlated with each other ($\rho\!=\!0.984$, $p < 0.001$), while neither shows evidence of a monotonic association with PR-AUC ($|\rho| < 0.08$, $p > 0.8$).
    In these configurations, the fixed-threshold metrics are strongly influenced by whether predicted probabilities cross 0.5 and do not reproduce the ranking obtained from the full probability curves.
\end{itemize}

These findings support the use of PR-AUC as the primary model-selection metric in this imbalanced classification setting. $F_1$ and $F_2$ become informative for the intended operating point only after the threshold has been selected on development data (Section~\ref{sec:results_threshold_xgb}).
Figure~\ref{fig:metric_heatmap} visualises the cross-validation metric values for all 13 configurations, sorted by PR-AUC; the disconnection between threshold-free and threshold-dependent metrics is clearly visible.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=1\linewidth]{images/figures/step5v2_metric_heatmap.png}
  \caption[Cross-validation metrics heatmap, 13 configurations]{Heatmap of cross-validation metric values for all 13 model $\times$ mode configurations (6-month, DEV set). Models sorted by PR-AUC in descending order. The disconnection between threshold-free (PR-AUC, ROC-AUC) and threshold-dependent ($F_1$, $F_2$ at 0.5) metrics is clearly visible.}
  \label{fig:metric_heatmap}
\end{figure}


\clearpage 
% ============================================================
\section{Feature Block Ablation}
\label{sec:results_ablation_v2}
% ============================================================

The feature-block analysis applies the hyperparameters tuned on the full feature set to each reduced configuration. It therefore describes the sensitivity of each fitted model family to the available feature blocks without re-optimising the model for every subset.

Four selected model configurations were evaluated across six feature configurations:
\textbf{A}~(core baseline variables, $n\!=\!15$),
\textbf{B}~(A + vitals, $n\!=\!27$),
\textbf{C}~(A + FVC, $n\!=\!18$),
\textbf{D}~(A + vitals + FVC, $n\!=\!30$),
\textbf{E}~(D + treatment, $n\!=\!33$),
\textbf{F}~(full set with labs, $n\!=\!35$).
Blocks B and C are parallel extensions of A rather than consecutive steps. D combines both blocks, while E and F then add treatment and laboratory variables sequentially.

\input{images/tables/step5v2_ablation.tex}

Figure~\ref{fig:ablation_v2} compares the PR-AUC profile across feature configurations for each model.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.85\linewidth]{images/figures/step5v2_ablation_prauc.png}
  \caption[Feature block ablation PR-AUC curves]{Feature-block analysis on the six-month DEV set using hyperparameters tuned on the full feature configuration. Points show mean PR-AUC and error bars show the standard deviation across five folds. B and C are alternative extensions of A; D combines both before treatment and laboratory variables are added in E and F.}
  \label{fig:ablation_v2}
\end{figure}

\noindent\textbf{Key observations.}
\begin{enumerate}
  \item \textbf{The core baseline block contains most of the observed performance.}
    PR-AUC with configuration A ranges from 0.395 for Logistic Regression to 0.434 for LightGBM. Across models, the best configuration improves on A by between 0.000 and 0.025, indicating that the additional blocks produce relatively small changes compared with the information already present at baseline.
  \item \textbf{The effect of FVC is model-dependent.}
    Adding FVC without vital signs (A~$\to$~C) increases PR-AUC from 0.409 to 0.434 for Random Forest and from 0.420 to 0.430 for XGBoost. The corresponding changes are smaller for Logistic Regression ($+0.005$) and slightly negative for LightGBM ($-0.002$). FVC therefore provides the clearest gain for the two unbalanced tree ensembles in this comparison, consistent with its established prognostic relevance in ALS~\cite{Papaiz2022}.
  \item \textbf{Vital signs do not produce a consistent improvement.}
    From A to B, PR-AUC is essentially unchanged for XGBoost and Random Forest, increases by 0.010 for Logistic Regression, and decreases by 0.014 for LightGBM. Combining vital signs with FVC in D reduces the fold-to-fold standard deviation relative to C for all four models, but does not consistently increase the mean.
  \item \textbf{Treatment and laboratory blocks have small and variable effects.}
    Treatment variables change PR-AUC by between $-0.009$ and $+0.001$ from D to E. Adding the two laboratory variables from E to F increases PR-AUC by 0.011 for XGBoost and 0.014 for Random Forest, is nearly neutral for Logistic Regression, and decreases it by 0.007 for LightGBM. From D to F, the net changes are $+0.002$, $+0.013$, $+0.001$, and $-0.006$, respectively.
  \item \textbf{No feature configuration is best for every model.}
    The full set F gives the highest mean PR-AUC for XGBoost (0.434) and Logistic Regression (0.407). Random Forest reaches its maximum with A + FVC (C: 0.434), while LightGBM performs best with the core block alone (A: 0.434). These differences are small relative to the fold standard deviations and were not subjected to separate significance tests.
\end{enumerate}

Overall, the analysis suggests that the core baseline variables account for most of the attainable discrimination in these four configurations. Additional blocks alter performance modestly and differently across model families; among them, FVC shows the clearest improvement for Random Forest and XGBoost.


% ============================================================
\section{3-Month Horizon: Tuning Results and Cross-Horizon Comparison}
\label{sec:results_3m}
% ============================================================

To examine the sensitivity of the modelling results to the prediction horizon, we repeated the Optuna tuning procedure for the 3-month horizon ($H\!=\!90 \pm 7$\,d). The analysis included the four highest-ranked model families from the 6-month comparison---XGBoost, Random Forest, LightGBM, and Logistic Regression---in balanced and unbalanced modes (eight configurations, 60 trials each; DEV $n\!=\!1{,}925$).

\subsection{3-Month Tuning Results}
\label{subsec:results_3m_tuning}

Table~\ref{tab:step5v2_tuning_3m} and Figure~\ref{fig:prauc_barchart_3m} present the 3-month results.

\input{images/tables/step5v2_tuning_comparison_3m.tex}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/step5v2_3m_prauc_barchart.png}
  \caption[Cross-validation PR-AUC by model, 3-month]{PR-AUC comparison of Optuna-tuned models (3-month, DEV, $n\!=\!1{,}925$). Blue: unbalanced; orange: balanced. Numerical annotations show mean PR-AUC.}
  \label{fig:prauc_barchart_3m}
\end{figure}

\clearpage
\noindent\textbf{Key observations.}
\begin{enumerate}
  \item \textbf{The leading configurations are closely matched.}
    Balanced XGBoost achieves the highest mean PR-AUC ($0.426 \pm 0.036$), followed by unbalanced XGBoost and unbalanced LightGBM (both $0.423$). The difference between the first four configurations is less than 0.004 and is small relative to their fold-to-fold standard deviations.
  \item \textbf{Class weighting has little effect on PR-AUC for the boosted models.}
    Balanced XGBoost exceeds its unbalanced counterpart by 0.003, whereas unbalanced LightGBM exceeds balanced LightGBM by 0.001. As in the six-month analysis, the much larger differences in $F_1$ and $F_2$ at a fixed threshold of 0.5 primarily reflect changes in the probability scale and operating point.
  \item \textbf{The exact ranking changes between horizons.}
    XGBoost provides the leading configuration at both horizons and Logistic Regression remains at the lower end of the comparison. However, LightGBM moves above Random Forest at three months, so the results support broad agreement at the extremes rather than an identical model ordering.
\end{enumerate}

\subsection{Cross-Horizon Comparison (3m vs 6m)}
\label{subsec:results_3m_vs_6m}


Table~\ref{tab:3m_vs_6m} and Figures~\ref{fig:3m_vs_6m_grouped}--\ref{fig:3m_vs_6m_scatter} compare PR-AUC across the two horizons for matched model configurations.

\input{images/tables/step5v2_3m_vs_6m_comparison.tex}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/step5v2_3m_vs_6m_prauc.png}
  \caption[PR-AUC comparison: 3-month vs.\ 6-month]{Descriptive PR-AUC comparison between 3-month and 6-month horizons for matched model $\times$ mode configurations tuned independently on their respective DEV sets. Annotations show mean PR-AUC values.}
  \label{fig:3m_vs_6m_grouped}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.65\linewidth]{images/figures/step5v2_3m_vs_6m_scatter.png}
  \caption[Scatter plot: 3-month vs.\ 6-month PR-AUC]{Descriptive comparison of mean PR-AUC at 3 and 6 months for each matched model configuration. Points above the parity line have a higher value at 3 months; points below it have a higher value at 6 months. Numerical annotations show $\Delta = \text{PR-AUC}_{3\text{m}}-\text{PR-AUC}_{6\text{m}}$.}
  \label{fig:3m_vs_6m_scatter}
\end{figure}

%\break

\noindent\textbf{Key findings.}
\begin{enumerate}
  \item \textbf{The observed PR-AUC differences are modest.}
    Across the eight matched configurations, $|\Delta|$ ranges from 0.002 to 0.021. The largest decrease at three months occurs for unbalanced Random Forest ($\Delta=-0.021$), while the largest increase occurs for unbalanced LightGBM ($\Delta=+0.008$).
  \item \textbf{Six-month PR-AUC is slightly higher on average, but not uniformly so.}
    Five configurations favour six months and three favour three months. The mean across the matched configurations is 0.419 at six months and 0.414 at three months, a difference of 0.006. These changes are smaller than the cross-validation standard deviation of every individual configuration.
  \item \textbf{Metric behaviour differs across the two analyses.}
    All eight matched configurations have higher mean ROC-AUC at three months, whereas PR-AUC changes in both directions. Because the horizons use different eligible cohorts, folds, slope estimates, and fold-level class distributions, the contribution of any one of these factors cannot be isolated from this comparison.
  \item \textbf{XGBoost leads at both horizons, although the selected mode differs.}
    The highest mean PR-AUC is obtained by unbalanced XGBoost at six months and balanced XGBoost at three months. This supports the use of XGBoost in the downstream six-month analysis, but does not establish an identical ranking across horizons.
\end{enumerate}

The \textbf{6-month horizon} is retained for the downstream analyses because its slopes are estimated from a longer follow-up period and more observations per participant, while its cross-validated PR-AUC is not lower overall in the matched comparison. The three-month results show that the same pipeline can be applied at a shorter horizon and produces a similar range of PR-AUC values. They should nevertheless be interpreted as a sensitivity analysis, rather than as external validation or evidence that performance generalises unchanged across horizons.


% ============================================================
\section{Statistical Comparison of Models}
\label{sec:results_statistical_comparison}
% ============================================================

The best configuration from each model family was re-evaluated on the same five GroupKFold splits. This produced paired fold-level PR-AUC scores for an exploratory comparison of the seven selected configurations.

\subsection{Per-Fold Performance}

Figure~\ref{fig:step7_fold_boxplot} shows the per-fold PR-AUC distribution for each model.
Mean PR-AUC ranges from 0.434 for unbalanced XGBoost to 0.387 for unbalanced KNN. The fold-level standard deviations range from 0.036 to 0.055, and the distributions overlap across all seven configurations.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.88\linewidth]{images/figures/step7_fold_prauc_boxplot.pdf}
  \caption[Per-fold PR-AUC distribution, 5-fold CV]{Per-fold PR-AUC for the seven best-per-family models across 5-fold GroupKFold on the DEV set (6-month horizon).
  Individual fold values are annotated; red dashed lines indicate the mean.
  XGBoost (unbalanced) has the highest mean PR-AUC (0.434), and the distributions overlap across models.}
  \label{fig:step7_fold_boxplot}
\end{figure}

\subsection{Friedman and Pairwise Comparisons}

The Friedman test gives $\chi^2_F(6)=12.43$ and $p=0.053$. The null hypothesis of equal rank distributions is therefore not rejected at $\alpha=0.05$. Kendall's coefficient is $W=0.414$, indicating some agreement among folds in their model rankings, but the five-fold design provides limited power and the asymptotic $p$-value lies close to the chosen threshold.

The exploratory Wilcoxon comparisons are reported in Appendix~\ref{app:stats}. The smallest raw two-sided $p$-value is 0.0625, observed for XGBoost versus Logistic Regression, XGBoost versus Decision Tree, and Random Forest versus KNN. All 21 Bonferroni-adjusted values are 1.0. This result is largely determined by the discrete resolution of a signed-rank test with five pairs and the number of simultaneous comparisons; it should not be interpreted as demonstrating equivalence among the models.

Unbalanced XGBoost remains the selected configuration because it has the highest mean cross-validated PR-AUC under the prespecified selection criterion. Its advantage over unbalanced Random Forest is only 0.003, and the statistical analysis does not establish that this difference is systematic. The selection should therefore be understood as a reproducible ranking decision within this development dataset, rather than evidence that XGBoost is intrinsically superior to the other leading models.


% ============================================================
\section{Threshold Optimisation}
\label{sec:results_threshold_xgb}
% ============================================================

The default threshold of $t\!=\!0.50$ produces very few positive predictions for the selected unbalanced XGBoost model. Alternative thresholds are therefore compared using out-of-fold (\gls{OOF}) predictions from five-fold GroupKFold on the DEV set. Labels for this analysis use the fixed DEV 30th-percentile slope cutoff ($-1.243$ points per 30 days).

Three optimality criteria are evaluated:
\begin{enumerate}
  \item \textbf{$F_2$-optimal} ($t\!=\!0.05$): $F_2=0.682$. At the lower boundary of the threshold grid, all 1,113 DEV participants are classified as rapid, giving recall 1.00 and precision 0.300. This solution provides no separation into predicted classes.
  \item \textbf{$F_1$-optimal} ($t\!=\!0.21$): $F_1=0.479$, with recall 0.874 and precision 0.330. This threshold classifies 79.6\% of DEV as rapid.
  \item \textbf{Youden's~$J$} ($t\!=\!0.35$): $J=0.171$, with recall 0.410 and precision 0.424. This criterion gives equal importance to sensitivity and specificity.
\end{enumerate}

$t=0.21$ is retained as the primary operating point because it maximises $F_1$ within the evaluated grid without collapsing to an all-positive classification. It nevertheless represents an aggressive rule: almost four in five development participants are flagged, and precision is only three percentage points above the rapid-progression prevalence. The threshold is therefore used to evaluate a sensitivity-oriented prototype and is not presented as a clinically established cutoff.
Figure~\ref{fig:step6_threshold_sweep} illustrates the multi-metric threshold sweep.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.70\linewidth]{images/figures/step6_threshold_sweep.png}
  \caption[Threshold sweep on DEV out-of-fold predictions]{Threshold sweep on DEV out-of-fold predictions for XGBoost (unbalanced, 6\,m).
  The $F_1$-optimal threshold ($t\!=\!0.21$, blue dotted) and Youden's~$J$ operating point ($t\!=\!0.35$, purple dotted) are annotated.}
  \label{fig:step6_threshold_sweep}
\end{figure}


% ============================================================
\section{Held-Out Test Set Evaluation}
\label{sec:results_test_eval}
% ============================================================

The seven configurations selected within each model family are fitted on the full DEV partition and evaluated once on the held-out TEST partition ($n\!=\!279$, rapid prevalence $30.1$\%).
Labels are defined using the 30th-percentile slope cutoff computed on the DEV set ($-1.243$~points per 30 days), ensuring no information leaks from the test set into the labelling process.

\subsection{Discrimination Metrics}

Table~\ref{tab:step6_test_metrics} reports threshold-free metrics (PR-AUC, ROC-AUC) and threshold-dependent metrics at the default $t\!=\!0.50$.

\begin{table}[ht!]
\centering
\caption[Held-out TEST set metrics, 6-month]{Held-out TEST set metrics (6-month horizon, $n\!=\!279$) from models fitted on DEV only. PR-AUC and ROC-AUC are threshold-free; $F_1$ and $F_2$ are evaluated at the default $t\!=\!0.50$; the Brier score measures probabilistic error (lower is better).}
\label{tab:step6_test_metrics}
\small
\begin{tabular}{l c c c c c c}
\toprule
Model & Mode & PR-AUC & ROC-AUC & $F_1$ & $F_2$ & Brier \\
\midrule
  RandomForest  & unbal. & \textbf{0.459} & 0.664 & 0.000 & 0.000 & 0.199 \\
  XGBoost       & unbal. & 0.456 & 0.660 & 0.085 & 0.058 & \textbf{0.197} \\
  SVM           & bal.   & 0.447 & \textbf{0.675} & 0.000 & 0.000 & 0.198 \\
  LightGBM      & bal.   & 0.429 & 0.637 & 0.422 & 0.440 & 0.224 \\
  LogisticReg.  & bal.   & 0.421 & 0.671 & \textbf{0.484} & \textbf{0.520} & 0.231 \\
  KNeighbors    & unbal. & 0.393 & 0.583 & 0.046 & 0.029 & 0.206 \\
  DecisionTree  & bal.   & 0.373 & 0.569 & 0.417 & 0.417 & 0.270 \\
\bottomrule
\end{tabular}
\end{table}\hfill \break

\noindent\textbf{Key observations:}
\begin{enumerate}
  \item \textbf{PR-AUC on held-out data.}
    Random Forest (PR-AUC\,$=\!0.459$) and XGBoost ($0.456$) both score slightly above their cross-validated development means ($0.431$ and $0.434$, respectively). This provides no evidence of a marked performance drop on the TEST partition, although a single held-out estimate cannot establish the absence of overfitting.
  \item \textbf{SVM has the highest ROC-AUC point estimate} ($0.675$), followed closely by Logistic Regression ($0.671$), Random Forest ($0.664$), and XGBoost ($0.660$).
    Its PR-AUC is lower than that of Random Forest and XGBoost, and it produces no positive predictions at $t\!=\!0.50$. The latter reflects an unsuitable default operating threshold rather than an absence of ranking ability.
  \item \textbf{Default threshold is unsuitable for unbalanced models.}
    XGBoost, RF, and KNN (all trained without class weighting) produce near-zero $F_1$ and $F_2$ at $t\!=\!0.50$, because their predicted probabilities concentrate below $0.50$ for the minority class.
    This reinforces the need for threshold optimisation (Section~\ref{sec:results_threshold_xgb}).
  \item \textbf{Class weighting changes default-threshold behaviour.}
    Balanced Logistic Regression achieves the highest $F_1$ ($0.484$) and $F_2$ ($0.520$) at $t\!=\!0.50$. These values describe performance at a common cutoff and do not replace the threshold-free criteria used for model selection.
\end{enumerate}

\noindent\textbf{Relation to model selection.}
XGBoost (unbalanced) had already been selected from the \gls{DEV} results, where it achieved the highest cross-validated PR-AUC. Random Forest obtains the highest TEST PR-AUC by a margin of 0.002, but the held-out results are not used to repeat model selection. XGBoost therefore remains the prespecified model for the subsequent analyses. Its Brier score ($0.197$) is also the lowest point estimate among the seven TEST evaluations, although it is nearly identical to SVM ($0.198$) and Random Forest ($0.199$).
Figure~\ref{fig:step6_test_prauc_bar} presents a visual comparison of PR-AUC across all models.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\linewidth]{images/figures/step6_test_prauc_bar.png}
  \caption[TEST set PR-AUC by model]{PR-AUC on the held-out TEST set (6\,m) for the seven best-per-family models.
  The dashed line indicates the baseline prevalence (0.30).}
  \label{fig:step6_test_prauc_bar}
\end{figure} 

\subsection{PR and ROC Curves}

Figures~\ref{fig:step6_pr_curves} and~\ref{fig:step6_roc_curves} present the precision--recall and ROC curves for all models on the TEST set.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.82\linewidth]{images/figures/step6_pr_curves.png}
  \caption[Precision--Recall curves on TEST set]{Precision--Recall curves on the held-out TEST set (6\,m). The dashed line indicates the baseline prevalence ($0.30$).}
  \label{fig:step6_pr_curves}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.82\linewidth]{images/figures/step6_roc_curves.png}
  \caption[ROC curves on TEST set]{ROC curves on the held-out TEST set (6\,m). The dashed line indicates the random classifier.}
  \label{fig:step6_roc_curves}
\end{figure}


\subsection{Threshold Evaluation on TEST}

$t=0.21$, selected from DEV OOF predictions (Section~\ref{sec:results_threshold_xgb}), is applied unchanged to the TEST set.
Table~\ref{tab:step6_threshold_test} compares XGBoost performance at multiple thresholds.

\begin{table}[ht!]
\centering
\caption[XGBoost performance at different thresholds]{DEV-fitted XGBoost (unbalanced, 6\,m) performance on TEST at different decision thresholds.}
\label{tab:step6_threshold_test}
\small
\begin{tabular}{c c c c c c c c c}
\toprule
$t$ & Precision & Recall & $F_1$ & $F_2$ & TP & FP & FN & TN \\
\midrule
0.21 & 0.351 & 0.857 & 0.498 & 0.665 & 72 & 133 & 12 & 62 \\
0.30 & 0.458 & 0.524 & 0.489 & 0.509 & 44 & 52  & 40 & 143 \\
0.35 & 0.500 & 0.405 & 0.447 & 0.421 & 34 & 34  & 50 & 161 \\
0.50 & 0.400 & 0.048 & 0.085 & 0.058 & 4  & 6   & 80 & 189 \\
\bottomrule
\end{tabular}
\end{table}

\noindent At the chosen threshold $t\!=\!0.21$:
\begin{itemize}
  \item The model identifies 72 of 84 rapid progressors (\textbf{recall\,$=\!85.7$\%}), missing only 12.
  \item It classifies 205 of 279 participants (73.5\%) as rapid. Of these, 133 are false positives, giving precision of 35.1\%.
  \item $F_1\!=\!0.498$ and $F_2\!=\!0.665$. Recall and precision are close to the corresponding DEV values, although this is a single held-out evaluation.
\end{itemize}

\noindent At the DEV-derived Youden threshold ($t\!=\!0.35$), TEST precision rises to 50.0\%, but recall falls to 40.5\%. The comparison illustrates the consequence of the selected operating point: $t=0.21$ retains substantially more rapid progressors at the cost of many additional false positives. Whether this trade-off is acceptable would require an externally defined clinical use case and prospective assessment; those conditions are not established in the present retrospective study.


\subsection{Calibration Analysis}

The probabilistic predictions of the DEV-fitted XGBoost model are examined using a reliability diagram and the Brier score (Figure~\ref{fig:step6_calibration}). Its Brier score is \textbf{0.197}, compared with approximately 0.210 for a constant prediction equal to the TEST prevalence. The reliability curve is close to the diagonal in the lower populated bins but departs from it at higher predicted probabilities, where fewer observations are available. The result indicates a modest reduction in mean squared probabilistic error relative to the constant reference, but does not demonstrate full calibration.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.55\linewidth]{images/figures/step6_calibration.png}
  \caption[XGBoost calibration on TEST set]{Reliability diagram for the DEV-fitted XGBoost model on TEST. The dashed line indicates perfect calibration; only probability bins containing observations are shown.}
  \label{fig:step6_calibration}
\end{figure}
\newpage
\subsection{Confusion Matrix}

Figure~\ref{fig:step6_cm} presents the confusion matrix at the $F_1$-optimal threshold ($t\!=\!0.21$).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.50\linewidth]{images/figures/step6_confusion_matrix.png}
  \caption[XGBoost confusion matrix at $t{=}0.21$]{Confusion matrix for the DEV-fitted XGBoost model on TEST at the DEV-derived threshold $t\!=\!0.21$.}
  \label{fig:step6_cm}
\end{figure}

\subsection{Post-Evaluation Refit}

After completion of the held-out evaluation and the TEST-based analyses reported in this chapter, the selected XGBoost pipeline is refitted on all 1,392 six-month participants for use by the prototype reporting workflow. This artifact uses the full-cohort slope cutoff ($-1.248$ points per 30 days) and retains the DEV-derived operating threshold $t=0.21$. A balanced Logistic Regression pipeline is refitted in parallel. No performance estimate is reported from these full-cohort fits, since they include the former TEST participants in training.

% ============================================================
\section{Explainability Analysis}
\label{sec:results_shap}
% ============================================================

This section examines the XGBoost model fitted on DEV using three complementary approaches: SHAP (global and local), LIME (local), and Logistic Regression coefficient analysis.
The XGBoost explanations are computed for the held-out TEST set ($N\!=\!279$), without refitting the model on those participants.

% ------------------------------------------------------------
\subsection{SHAP --- Global Explanations}
\label{subsec:results_shap_global}
% ------------------------------------------------------------

Figure~\ref{fig:shap_bar_global} ranks features by mean absolute SHAP value across all TEST-set subjects.
The baseline ALSFRS-R total score ranks first, with a mean $|\text{SHAP}|$ of \textbf{0.292}, more than twice that of FVC in litres (0.123).
Age (0.093), respiratory rate (0.086), pulse (0.073), ALT (0.061), and FVC as percentage of normal (0.054) follow.
The ranking indicates that functional and respiratory measurements carry most of the model's attribution magnitude, while variables from the other blocks make smaller contributions.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\linewidth]{images/figures/shap_bar_global.pdf}
  \caption[SHAP global feature importance, top 15]{Mean absolute SHAP values (top~15 features) for the DEV-fitted XGBoost model on the TEST set. Baseline ALSFRS-R total score ranks first, followed by FVC in litres and age.}
  \label{fig:shap_bar_global}
\end{figure}
\newpage
The beeswarm plot (Figure~\ref{fig:shap_beeswarm}) reveals the direction of these effects.
Lower ALSFRS-R scores generally shift predictions towards the rapid class, whereas higher scores tend to shift them towards the slow class.
Lower FVC values show a similar pattern.
The effects of age, respiratory rate, and the remaining variables are less uniform, which is compatible with non-linearities and interactions in the fitted model.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\linewidth]{images/figures/shap_beeswarm.pdf}
  \caption[SHAP beeswarm plot, XGBoost TEST set]{SHAP beeswarm plot for the DEV-fitted XGBoost model on TEST. Each dot represents one subject; colour indicates the observed feature value (red\,=\,high, blue\,=\,low). Positive SHAP values shift the prediction towards the rapid class.}
  \label{fig:shap_beeswarm}
\end{figure}

\clearpage
% ------------------------------------------------------------
\subsection{SHAP --- Local Explanations (Case Studies)}
\label{subsec:results_shap_local}
% ------------------------------------------------------------

To illustrate how the model reasons about individual patients, we select one representative subject from each confusion-matrix quadrant, choosing the patient closest to the median predicted probability within that quadrant.
Table~\ref{tab:case_study_profiles} summarises the clinical profiles of the four selected patients.

\begin{table}[H]
\centering
\caption[Clinical profiles of four case-study subjects]{Clinical profiles of the four case-study subjects selected from each confusion-matrix quadrant.}
\label{tab:case_study_profiles}
\small
\begin{tabular}{l c c c c}
\toprule
 & \textbf{TP} & \textbf{FP} & \textbf{FN} & \textbf{TN} \\
\midrule
Subject ID      & 672752 & 491257 & 330379 & 631435 \\
$P(\text{rapid})$ & 0.334 & 0.284 & 0.169 & 0.177 \\
ALSFRS-R (t0)   & 43     & 38     & 39     & 45     \\
Age              & 54     & 28     & 56     & 39     \\
FVC (\% normal)  & --     & --     & 79     & 87     \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{True Positive (Subject~672752).}
Figure~\ref{fig:shap_wf_tp} shows the SHAP waterfall for this correctly identified rapid progressor.
The high ALSFRS-R score (43) shifts the prediction towards the slow class.
This is offset by respiratory rate, the imputed FVC value, temperature, and several smaller contributions, leaving the predicted probability above the operating threshold.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.63\linewidth]{images/figures/shap_waterfall_TP.pdf}
  \caption[SHAP waterfall: true positive case (Subject 672752)]{SHAP waterfall plot for the TP case study (Subject~672752). Respiratory rate and several smaller contributions offset the effect of a high baseline ALSFRS-R score.}
  \label{fig:shap_wf_tp}
\end{figure} \break

\noindent\textbf{False Positive (Subject~491257).}
This participant was classified as rapid despite the strong slow-class contribution associated with age 28 (Figure~\ref{fig:shap_wf_fp}).
BMI, ALSFRS-R, the imputed FVC value, and respiratory rate collectively shift the prediction in the opposite direction and place it above the threshold.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.62\linewidth]{images/figures/shap_waterfall_FP.pdf}
  \caption[SHAP waterfall: false positive case (Subject 491257)]{SHAP waterfall plot for the FP case study (Subject~491257). Several moderate contributions outweigh the slow-class contribution associated with younger age.}
  \label{fig:shap_wf_fp}
\end{figure}

\noindent\textbf{False Negative (Subject~330379).}
This rapid progressor was missed by the model (Figure~\ref{fig:shap_wf_fn}).
Low ALT, an ALSFRS-R score of 39, temperature, and several smaller effects shift the prediction towards the slow class, resulting in a probability of 0.169.
The case illustrates that the baseline profile can appear insufficiently suggestive of the decline observed during follow-up.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.62\linewidth]{images/figures/shap_waterfall_FN.pdf}
  \caption[SHAP waterfall: false negative case (Subject 330379)]{SHAP waterfall plot for the FN case study (Subject~330379). The combined baseline contributions shift the prediction below the selected threshold.}
  \label{fig:shap_wf_fn}
\end{figure}

\noindent\textbf{True Negative (Subject~631435).}
For this correctly classified slow progressor (Figure~\ref{fig:shap_wf_tn}), high ALSFRS-R (45), age 39, and pulse 60 provide the largest contributions towards the slow class.
These effects outweigh the positive contributions from BMI, the imputed FVC value, and temperature.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.62\linewidth]{images/figures/shap_waterfall_TN.pdf}
  \caption[SHAP waterfall: true negative case (Subject 631435)]{SHAP waterfall plot for the TN case study (Subject~631435). Baseline function, age, and pulse provide the largest contributions towards the slow class.}
  \label{fig:shap_wf_tn}
\end{figure}

\clearpage
% ------------------------------------------------------------
\subsection{LIME --- Local Explanations}
\label{subsec:results_lime}
% ------------------------------------------------------------

LIME is applied to the same four case-study subjects to provide a complementary local approximation of the classifier.
LIME generates 2{,}000 perturbed samples around each instance and fits a local sparse linear model to approximate the classifier's decision boundary.
Figures~\ref{fig:lime_tp}--\ref{fig:lime_tn} present the LIME explanations for each quadrant.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.72\linewidth]{images/figures/lime_local_TP.pdf}
  \caption[LIME explanation: true positive case]{LIME explanation for the TP case study (Subject~672752). Respiratory rate shifts the local surrogate towards the rapid class, whereas high ALSFRS-R shifts it towards the slow class.}
  \label{fig:lime_tp}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.72\linewidth]{images/figures/lime_local_FP.pdf}
  \caption[LIME explanation: false positive case]{LIME explanation for the FP case study (Subject~491257). The local surrogate assigns opposing contributions to age and several respiratory and anthropometric variables.}
  \label{fig:lime_fp}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.72\linewidth]{images/figures/lime_local_FN.pdf}
  \caption[LIME explanation: false negative case]{LIME explanation for the FN case study (Subject~330379). Respiratory rate and several baseline variables shift the local approximation towards the slow class.}
  \label{fig:lime_fn}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.72\linewidth]{images/figures/lime_local_TN.pdf}
  \caption[LIME explanation: true negative case]{LIME explanation for the TN case study (Subject~631435). High ALSFRS-R, lower respiratory rate, age, and pulse shift the local approximation towards the slow class.}
  \label{fig:lime_tn}
\end{figure}

\clearpage
% ------------------------------------------------------------
\subsection{SHAP--LIME Concordance}
\label{subsec:results_concordance}
% ------------------------------------------------------------

Table~\ref{tab:concordance} compares the top-10 features identified by SHAP and LIME for each case study.
Across the four quadrants, 5--7 features overlap between the two local rankings.
ALSFRS-R appears in every overlap set, while age, FVC, respiratory rate, pulse, BMI, and temperature recur in subsets of cases.

\begin{table}[ht!]
\centering
\caption[SHAP--LIME top-10 feature overlap]{Number of overlapping features in the top-10 SHAP vs.\ LIME rankings for each case-study subject.}
\label{tab:concordance}
\small
\begin{tabular}{l c l}
\toprule
\textbf{Case} & \textbf{Overlap} & \textbf{Shared features} \\
\midrule
TP & 5 & ALSFRS-R, FVC (litres), Pulse, Resp.\ Rate, Temperature \\
FP & 6 & ALSFRS-R, Age, BMI, FVC (litres), Pulse, Resp.\ Rate \\
FN & 7 & ALSFRS-R, ALT, Age, BMI, FVC (\% normal), Resp.\ Rate, Temperature \\
TN & 6 & ALSFRS-R, Age, BMI, FVC (litres), Pulse, Temperature \\
\bottomrule
\end{tabular}
\end{table}

\noindent The overlap ranges from 50\% to 70\%.
The disagreement that remains is unsurprising: TreeSHAP attributes the fitted tree model's output, whereas LIME approximates the decision surface around each case using perturbed observations.
Their shared features therefore indicate partial agreement about the local prediction, but do not establish causal or clinical validity.

% ------------------------------------------------------------
\subsection{Logistic Regression Coefficients}
\label{subsec:results_lr_coefs}
% ------------------------------------------------------------

As a complementary, model-intrinsic explanation, we analyse the coefficients of the balanced Logistic Regression model (Figure~\ref{fig:lr_coefs}).
Numeric features are standardised prior to fitting, whereas one-hot encoded indicators remain binary; coefficient magnitudes are therefore most directly comparable within the same feature type.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.80\linewidth]{images/figures/lr_coefficients.pdf}
  \caption[Logistic regression coefficients]{Coefficients of the balanced Logistic Regression model (top~15 by absolute value). Numeric features are standardised before fitting, while one-hot encoded indicators remain binary. Negative coefficients (blue) reduce the predicted log-odds of rapid progression; positive coefficients (red) increase them.}
  \label{fig:lr_coefs}
\end{figure}
\newpage
\noindent The top three LR coefficients by absolute value are:
\begin{enumerate}
  \item \textbf{ALSFRS-R\_t0} ($\beta = -0.283$): higher baseline function is associated with lower predicted log-odds of rapid progression.
  \item \textbf{Respiratory\_Rate} ($\beta = +0.115$): higher respiratory rate is associated with higher predicted log-odds.
  \item \textbf{FVC\_Litres} ($\beta = -0.088$): higher forced vital capacity is associated with lower predicted log-odds.
\end{enumerate}

\noindent Functional status and respiratory measurements therefore recur in both the linear and non-linear models, although their exact ranking differs.
Age ($\beta = +0.076$), study-arm assignment ($\beta = -0.062$), FVC percentage of normal ($\beta = -0.052$), temperature ($\beta = -0.050$), and pulse ($\beta = +0.045$) also appear among the larger Logistic Regression coefficients.

\subsection{Summary of Explainability Findings}
\label{subsec:results_xai_summary}

Taken together, the three analyses show the following patterns:
\begin{itemize}
  \item \textbf{Baseline ALSFRS-R total score} has the largest global SHAP importance and the largest absolute Logistic Regression coefficient.
  \item \textbf{Respiratory markers} (FVC and respiratory rate) contribute prominently in both models and in several local explanations.
  \item \textbf{Age} and \textbf{vital signs} (pulse, temperature) contribute secondary predictive signals.
  \item The selected false-negative case receives several slow-class contributions despite subsequently meeting the rapid-progression definition, illustrating a limitation of relying on baseline measurements alone.
  \item Local SHAP--LIME overlap ranges from 50\% to 70\%. This partial agreement supports a consistent descriptive reading of several predictions, while leaving method-specific differences that should not be interpreted as validation.
\end{itemize}


% ============================================================
\section{Supplementary Analyses}
\label{sec:results_supplementary}
% ============================================================

This section presents six supplementary analyses of the DEV-fitted XGBoost model: error profiling, subgroup performance, decision curve analysis, learning curves, bootstrap confidence intervals, and partial dependence plots.
All analyses use the held-out TEST set ($N\!=\!279$) at the $F_1$-optimal threshold $t\!=\!0.21$.

% ------------------------------------------------------------
\subsection{Error Profiling}
\label{subsec:results_error_profile}
% ------------------------------------------------------------

Figures~\ref{fig:error_age}--\ref{fig:error_fvc} break down the confusion-matrix outcomes by age group, ALSFRS-R severity, sex, and FVC status.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/error_profile_age.pdf}
  \caption[Prediction outcomes by age group]{Distribution of prediction outcomes (TP, FP, FN, TN) by age group. Six of the 12 false negatives occur among participants aged 49 years or younger; missing age values are shown separately.}
  \label{fig:error_age}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/error_profile_alsfrs.pdf}
  \caption[Prediction outcomes by ALSFRS-R severity]{Distribution of prediction outcomes by ALSFRS-R severity. Seven of the 12 false negatives have a baseline ALSFRS-R score of at least 40, including five in the mildest group ($>42$).}
  \label{fig:error_alsfrs}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/error_profile_sex.pdf}
  \caption[Prediction outcomes by sex]{Distribution of prediction outcomes by sex. False negatives account for 4.7\% of female participants and 4.0\% of male participants.}
  \label{fig:error_sex}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/error_profile_fvc.pdf}
  \caption[Prediction outcomes by FVC percentage]{Distribution of prediction outcomes by FVC (\% of normal). FVC percentage is missing for 123 participants, who are retained as a separate group.}
  \label{fig:error_fvc}
\end{figure}
\newpage
\noindent\textbf{Key findings from the error analysis:}
\begin{itemize}
  \item Seven of the 12 false negatives have ALSFRS-R $\geq 40$, and five have scores above 42. This pattern is consistent with lower sensitivity among participants with relatively preserved baseline function, although false negatives also occur in the lower score groups.
  \item Six false negatives occur in the $\leq$49 age group. The remaining six are distributed across the three older groups, while none occurs among the 33 participants with missing age.
  \item FVC percentage is observed for only 156 participants. Among the seven false negatives with observed FVC, five have values above 77\%; the remaining five false negatives have missing FVC. These small and incomplete counts do not support a strong conclusion about concentration at high FVC.
  \item The false-negative proportions are similar for female and male participants (4.7\% and 4.0\%, respectively). This descriptive comparison does not, by itself, establish equivalence between sexes.
  \item False positives are numerous in every stratification, consistent with the low threshold's emphasis on sensitivity: the model produces 133 false positives while detecting 72 of 84 rapid progressors.
\end{itemize}

\clearpage
% ------------------------------------------------------------
\subsection{Subgroup Performance}
\label{subsec:results_subgroup}
% ------------------------------------------------------------

Table~\ref{tab:subgroup_metrics} and Figures~\ref{fig:subgroup_prauc}--\ref{fig:subgroup_recall} present PR-AUC and recall stratified by clinical subgroups.

\begin{table}[ht!]
\centering
\caption[Performance metrics by clinical subgroup]{Performance metrics by clinical subgroup on the TEST set ($t\!=\!0.21$).}
\label{tab:subgroup_metrics}
\small
\begin{tabular}{l l r r c c c c}
\toprule
\textbf{Stratification} & \textbf{Group} & \textbf{N} & \textbf{N\textsubscript{rapid}} & \textbf{PR-AUC} & \textbf{Recall} & \textbf{Prec.} & \textbf{$F_1$} \\
\midrule
Age & $\leq$49      & 65  & 22 & 0.437 & 0.727 & 0.421 & 0.533 \\
    & 50--57        & 68  & 22 & 0.567 & 0.955 & 0.382 & 0.545 \\
    & 58--65        & 59  & 14 & 0.375 & 0.786 & 0.244 & 0.373 \\
    & $>$65         & 54  & 15 & 0.365 & 0.867 & 0.310 & 0.456 \\
    & Missing       & 33  & 11 & 0.770 & 1.000 & 0.440 & 0.611 \\
\midrule
Sex & Female        & 106 & 32 & 0.450 & 0.844 & 0.310 & 0.454 \\
    & Male          & 173 & 52 & 0.473 & 0.865 & 0.381 & 0.529 \\
\midrule
ALSFRS-R & $\leq$35 & 72  & 36 & 0.558 & 0.972 & 0.522 & 0.680 \\
         & 36--39    & 73  & 24 & 0.418 & 0.833 & 0.333 & 0.476 \\
         & 40--42    & 60  & 13 & 0.380 & 0.846 & 0.244 & 0.379 \\
         & $>$42     & 74  & 11 & 0.206 & 0.545 & 0.182 & 0.273 \\
\midrule
FVC (\%) & $\leq$77\% & 40  & 16 & 0.590 & 0.938 & 0.417 & 0.577 \\
         & 78--87\%   & 34  & 15 & 0.678 & 0.800 & 0.462 & 0.585 \\
         & 88--98\%   & 37  & 7  & 0.306 & 0.857 & 0.250 & 0.387 \\
         & $>$98\%    & 45  & 9  & 0.317 & 0.778 & 0.304 & 0.438 \\
         & Missing    & 123 & 37 & 0.420 & 0.865 & 0.333 & 0.481 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/subgroup_pr_auc.pdf}
  \caption[TEST set PR-AUC by clinical subgroup]{PR-AUC by clinical subgroup on the TEST set. The dashed line indicates the overall TEST value (0.456). Missing age and FVC values are retained as separate groups. Differences should be interpreted alongside subgroup prevalence.}
  \label{fig:subgroup_prauc}
\end{figure}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/subgroup_recall.pdf}
  \caption[TEST set recall by clinical subgroup]{Recall by clinical subgroup on the TEST set ($t\!=\!0.21$). The dashed line indicates overall TEST recall (0.857). The lowest values occur for ALSFRS-R $>42$ (0.545) and age $\leq$49 (0.727).}
  \label{fig:subgroup_recall}
\end{figure}
\newpage
\noindent\textbf{Key observations:}
\begin{itemize}
  \item \textbf{ALSFRS-R severity}: recall decreases from 97.2\% in the $\leq$35 group to 54.5\% in the $>42$ group. This is the clearest subgroup difference and is consistent with the error profile, although the estimates are based on 36 and 11 rapid progressors, respectively.
  \item \textbf{FVC status}: absolute PR-AUC is higher in the two lower-FVC groups (0.590--0.678), which also have higher rapid-progressor prevalence (40.0--44.1\%) than the higher-FVC groups (18.9--20.0\%). The PR-AUC difference therefore cannot be interpreted independently of prevalence.
  \item \textbf{Sex}: estimates are close for females and males (PR-AUC 0.450 vs.\ 0.473; recall 0.844 vs.\ 0.865). These descriptive results do not constitute a formal test of fairness or equivalence.
  \item \textbf{Age}: observed-age groups vary in PR-AUC from 0.365 to 0.567 and in recall from 0.727 to 0.955. The missing-age group has higher estimates, but missingness may encode study or data-collection differences and is not interpreted as a clinical age effect.
  \item \textbf{Missing FVC}: the 123 participants without an observed FVC percentage have PR-AUC 0.420 and recall 0.865, showing why excluding this group would leave a substantial part of TEST unreported.
\end{itemize}

\clearpage
% ------------------------------------------------------------
\subsection{Decision Curve Analysis}
\label{subsec:results_dca}
% ------------------------------------------------------------

Figure~\ref{fig:decision_curve} presents the decision curve analysis for the DEV-fitted XGBoost model.
The curve compares the model's estimated net benefit with the treat-all and treat-none reference strategies across candidate threshold probabilities.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.72\linewidth]{images/figures/decision_curve.pdf}
  \caption[Decision Curve Analysis, TEST set]{Decision Curve Analysis for XGBoost on the TEST set. The shaded region marks the main continuous interval ($t=0.18$--$0.47$) in which the model has higher estimated net benefit than both reference strategies.}
  \label{fig:decision_curve}
\end{figure}

\noindent The principal continuous interval in which the model exceeds both reference strategies is $t \in [0.18,\,0.47]$.
Higher net benefit also occurs at the isolated grid points 0.13, 0.54, and 0.57; these are not treated as stable intervals, particularly at 0.54 and 0.57, where only three and one participants are classified as positive.
At the selected operating point $t=0.21$, the model's net benefit is 0.131, compared with 0.115 for treat-all and 0 for treat-none.
The difference of 0.016 relative to treat-all corresponds to approximately 1.6 additional net true-positive decisions per 100 participants under the threshold-implied weighting of false positives.
This result suggests potential decision value over a range that includes the selected threshold, but it does not establish clinical utility without an explicitly defined intervention, external validation, and uncertainty estimates.

% ------------------------------------------------------------
\subsection{Learning Curves}
\label{subsec:results_learning_curves}
% ------------------------------------------------------------

Figure~\ref{fig:learning_curve} shows PR-AUC and ROC-AUC on the TEST set as a function of training-set size.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.72\linewidth]{images/figures/learning_curve.pdf}
  \caption[XGBoost training-size sensitivity]{TEST-set performance after fitting XGBoost on progressively larger stratified subsets of DEV. Points below 100\% show the mean across 20 repetitions; error bars represent one standard deviation. The full DEV model is fitted once.}
  \label{fig:learning_curve}
\end{figure}

\noindent Mean PR-AUC is 0.424 at 20\% of DEV ($n=222$), 0.440 at 60\% ($n=667$), and 0.446 at 90\% ($n=1{,}001$); the model fitted on all 1{,}113 DEV participants reaches 0.456.
The corresponding standard deviation decreases from 0.029 at 20\% to 0.011 at 90\%, indicating greater stability as the training subset grows.
However, the error bars overlap across most adjacent fractions and the mean curve is not monotonic.
The results therefore suggest modest average gains and reduced variability with more DEV participants, rather than a clearly located plateau.
The deterministic full-data point is not directly comparable with the repeated-subset standard deviations, and this analysis cannot determine whether a larger independent cohort with the same features would yield further improvement.

% ------------------------------------------------------------
\subsection{Bootstrap Confidence Intervals}
\label{subsec:results_bootstrap}
% ------------------------------------------------------------

Table~\ref{tab:bootstrap_ci} and Figure~\ref{fig:bootstrap_ci} present 95\% bootstrap confidence intervals ($B\!=\!2{,}000$) for the key TEST-set metrics.

\begin{table}[ht!]
\centering
\caption[Bootstrap 95\% confidence intervals, TEST set]{Bootstrap 95\% confidence intervals for TEST-set metrics ($B\!=\!2{,}000$, $t\!=\!0.21$).}
\label{tab:bootstrap_ci}
\small
\begin{tabular}{l c c c}
\toprule
\textbf{Metric} & \textbf{Point estimate} & \textbf{95\% CI} & \textbf{CI width} \\
\midrule
PR-AUC    & 0.456 & [0.364,~0.568] & 0.204 \\
ROC-AUC   & 0.660 & [0.586,~0.730] & 0.144 \\
Recall    & 0.857 & [0.781,~0.929] & 0.148 \\
Precision & 0.351 & [0.283,~0.419] & 0.136 \\
$F_1$     & 0.498 & [0.423,~0.568] & 0.145 \\
$F_2$     & 0.665 & [0.593,~0.729] & 0.136 \\
Brier     & 0.197 & [0.174,~0.221] & 0.047 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.78\linewidth]{images/figures/bootstrap_ci.pdf}
  \caption[Forest plot of bootstrap confidence intervals]{Forest plot of percentile-bootstrap 95\% confidence intervals for TEST-set metrics ($B\!=\!2{,}000$). The intervals describe TEST-sample uncertainty conditional on the fitted model and fixed threshold.}
  \label{fig:bootstrap_ci}
\end{figure}
\newpage
\noindent\textbf{Key observations:}
\begin{itemize}
  \item PR-AUC has the widest interval, $[0.364,\,0.568]$, showing appreciable uncertainty around the point estimate of 0.456.
  \item Recall is estimated at 0.857 with a 95\% interval of $[0.781,\,0.929]$. The corresponding $F_2$ interval is $[0.593,\,0.729]$, reflecting uncertainty in screening-oriented threshold performance.
  \item Precision remains comparatively low across the bootstrap distribution, with an interval of $[0.283,\,0.419]$, consistent with the large number of false positives at $t=0.21$.
  \item The Brier interval is $[0.174,\,0.221]$. Its smaller numerical width is not directly comparable with those of discrimination or classification metrics because the measures have different sampling distributions and interpretations.
  \item These intervals quantify variation from resampling TEST participants only. They should not be read as covering the additional uncertainty introduced by model development and threshold selection.
\end{itemize}

% ------------------------------------------------------------
\subsection{Partial Dependence Plots}
\label{subsec:results_pdp}
% ------------------------------------------------------------

Figure~\ref{fig:pdp_top3} shows partial dependence plots for the three features with the highest global SHAP importance.

\begin{figure}[ht!]
  \centering
  \includegraphics[width=0.95\linewidth]{images/figures/pdp_top3.pdf}
  \caption[Partial Dependence Plots for top-3 SHAP features]{Partial dependence plots for the three highest-ranked global SHAP features: baseline ALSFRS-R total, FVC in litres, and age. Grids span the 5th--95th percentiles of observed TEST values; rug marks show observed data density.}
  \label{fig:pdp_top3}
\end{figure}
\newpage
\noindent The PDPs summarise how the model's average prediction changes when each feature is varied while the remaining preprocessed features are held at their observed values:
\begin{itemize}
  \item \textbf{ALSFRS-R total (baseline):} average predicted probability generally decreases across the observed range of 29--46, with several step changes typical of a tree ensemble. The total PDP range is 0.201.
  \item \textbf{FVC (litres):} predictions are relatively flat below approximately 3.7 litres, fall around 3.8--4.0 litres, and then stabilise. The total PDP range is 0.080. Only 162 of 279 TEST participants have observed FVC in litres, so this curve is supported by a smaller observed subset.
  \item \textbf{Age}: average predicted probability rises sharply around the low-40s and then remains broadly stable across much of the higher observed range. The total PDP range is 0.104; age is observed for 246 TEST participants.
\end{itemize}

\noindent These plots complement the SHAP ranking by showing the fitted model's marginal response shape rather than only attribution magnitude.
The ALSFRS-R pattern is compatible with the global SHAP result, but none of the curves should be interpreted as a causal effect.
Correlations among baseline function, respiratory measurements, age, and other covariates mean that some counterfactual combinations evaluated by a PDP may be rare in the observed cohort.

% ============================================================
\section{Patient Risk Report}
\label{sec:results_patient_report}
% ============================================================

The patient risk report generator (Section~\ref{sec:methods_patient_report}) was exercised using cohort records and fictitious profiles.
This analysis checks the software path from input data to model output, local explanation, and formatted report.
It is not an additional evaluation of discrimination or clinical utility.

\subsection{Cohort Record Reports}
\label{subsec:results_real_reports}

Twelve reports were generated for records originally assigned to \gls{TEST}.
Because the persisted model used by the generator was refitted on the complete cohort after held-out evaluation, these examples are in-sample with respect to that final artefact.
Table~\ref{tab:real_patient_reports} summarises the key results.

\begin{table}[ht!]
\centering
\caption[Reports for 12 cohort records]{Illustrative outputs for 12 cohort records originally assigned to TEST. P(rapid) denotes the probability produced by the full-cohort refit; Pred is the classification at $t=0.21$, and Outcome is the retrospectively derived target. These rows are descriptive examples, not held-out predictions.}
\label{tab:real_patient_reports}
\small
\begin{tabular}{lcccl}
\toprule
Patient ID & P(rapid) & Pred & Outcome & Top SHAP Feature \\
\midrule
666     & 45.3\% & Rapid & Rapid & ALSFRS-R (33) \\
524242  & 27.5\% & Rapid & Rapid & ALSFRS-R (39) \\
1125    & 54.2\% & Rapid & Slow  & ALSFRS-R (32) \\
476981  & 35.8\% & Rapid & Slow  & ALSFRS-R (37) \\
48027   & 36.0\% & Rapid & Slow  & Age (40) \\
326632  & 26.4\% & Rapid & Slow  & ALT (13) \\
582861  & 39.8\% & Rapid & Slow  & ALSFRS-R (35) \\
714820  & 20.8\% & Slow  & Slow  & ALSFRS-R (45) \\
113562  & 15.3\% & Slow  & Slow  & ALSFRS-R (44) \\
524909  & 18.1\% & Slow  & Slow  & Age (25) \\
505201  & 13.0\% & Slow  & Slow  & ALSFRS-R (45) \\
405398  & 14.2\% & Slow  & Slow  & ALSFRS-R (47) \\
\bottomrule
\end{tabular}
\end{table}

\noindent The selected records cover probabilities on both sides of the operating threshold and confirm that the generator formats the model output, retrospective outcome, and local explanation as intended.
Baseline ALSFRS-R is the largest absolute SHAP contributor in nine of the 12 reports; age leads in two and ALT in one.
This predominance is broadly consistent with the global ranking, while the exceptions illustrate that local attribution can differ across records.
No sensitivity, precision, or error rate is calculated from these examples because the final model has already seen these records.

\subsection{Fictitious Profile Reports}
\label{subsec:results_synthetic_reports}

Ten fictitious profiles were generated using the random profile generator (Section~\ref{subsec:methods_random_patient}) and processed through the report pipeline.
Table~\ref{tab:synthetic_patient_reports} summarises the results.

\begin{table}[ht!]
\centering
\caption[Reports for 10 fictitious profiles]{Illustrative report outputs for 10 fictitious profiles generated from approximate marginal distributions calibrated on PRO-ACT. The profiles are software test inputs and do not represent a clinical validation cohort.}
\label{tab:synthetic_patient_reports}
\small
\begin{tabular}{lccl}
\toprule
Patient & P(rapid) & Pred & Top SHAP Feature \\
\midrule
Random 001 & 30.7\% & Rapid & ALSFRS-R (29) \\
Random 002 & 23.1\% & Rapid & Respiratory rate (11) \\
Random 003 & 38.7\% & Rapid & Pulse (46) \\
Random 004 & 54.3\% & Rapid & ALSFRS-R (33) \\
Random 005 & 47.2\% & Rapid & ALSFRS-R (28) \\
Random 006 & 18.3\% & Slow  & Age (36) \\
Random 007 & 30.5\% & Rapid & Respiratory rate (11) \\
Random 008 & 24.8\% & Rapid & FVC (4.0~L) \\
Random 009 & 23.9\% & Rapid & FVC (4.9~L) \\
Random 010 & 33.4\% & Rapid & Respiratory rate (14) \\
\bottomrule
\end{tabular}
\end{table}
\newpage
\noindent All ten fictitious profiles were accepted by the persisted preprocessing and prediction pipeline and produced a complete report.
Their predicted probabilities range from 18\% to 54\%, with nine falling above the selected threshold.
This proportion should not be compared with cohort prevalence or model sensitivity: most variables are sampled separately, and the resulting profiles do not preserve the multivariable dependence structure of the source data.
The leading local contributor varies across these inputs: ALSFRS-R and respiratory rate each lead in three reports, FVC in two, and pulse and age in one each.
These observations describe the generated examples only.

\subsection{Summary}
The generator completed the intended technical workflow for both stored cohort records and fictitious \gls{JSON} inputs.
It combines the predicted probability and thresholded class with a local SHAP explanation and descriptive percentile flags.
The exercise demonstrates report generation, not clinical validity or readiness for decision support.

\clearpage
% ============================================================
\section{Summary of Key Findings}
\label{sec:results_summary}
% ============================================================

Study~I examined whether rapid six-month functional progression could be predicted from baseline \gls{PRO-ACT} data under a leakage-free evaluation protocol.
Its main findings can be summarised as follows:

\begin{enumerate}
  \item \textbf{The cohort supported the intended analysis, but data quality required active review.}
    The six-month cohort included 1,392 participants, and 99.8\% of the slopes were estimated from at least three \gls{ALSFRS-R} measurements.
    The preprocessing audit corrected a systematic height-unit error affecting 147 raw records and showed that \gls{FVC} availability varied across trials.
    The source of the missingness could not be established from the available data.

  \item \textbf{No single classifier was clearly superior.}
    Among 13 tuned configurations, unbalanced XGBoost ranked first on development \gls{PR-AUC} ($0.434 \pm 0.049$), narrowly ahead of unbalanced Random Forest ($0.431 \pm 0.042$) and balanced \gls{SVM} ($0.425 \pm 0.049$).
    The fold-level distributions overlapped, and the Friedman test did not reject equal model ranks ($p=0.053$).
    XGBoost was therefore selected by the prespecified ranking rule, not because statistical superiority had been demonstrated.

  \item \textbf{Baseline functional status contained most of the predictive signal.}
    The core feature block retained most of the cross-validated performance, while adding \gls{FVC} gave the clearest incremental gain for Random Forest and XGBoost.
    Vital signs, treatment variables, and laboratory measurements had smaller and less consistent effects.
    Repeating the analysis at three months produced a similar range of \gls{PR-AUC} values, although the model ordering changed slightly; six months remained the primary horizon because the slopes were supported by longer follow-up.

  \item \textbf{Held-out discrimination was moderate and uncertain.}
    The selected XGBoost model achieved a TEST \gls{PR-AUC} of 0.456, compared with a rapid-progression prevalence of 0.301.
    Random Forest was almost identical at 0.459, but the held-out partition was not used to reopen model selection.
    The bootstrap 95\% interval for XGBoost \gls{PR-AUC} was wide ($[0.364,\,0.568]$), and the Brier score of 0.197 indicated modest probabilistic accuracy rather than full calibration.

  \item \textbf{The operating threshold determined the practical behaviour of the model.}
    At the development-derived threshold of $t=0.21$, XGBoost detected 72 of 84 rapid progressors on TEST (recall 85.7\%) but classified 205 of 279 participants as rapid.
    Precision was 35.1\%, with 133 false positives, showing that the gain in sensitivity came at a substantial cost in specificity.
    The threshold is consequently an experimental, sensitivity-oriented operating point rather than a clinically established cutoff.

  \item \textbf{The explanations were coherent at population level but less uniform for individual records.}
    Baseline \gls{ALSFRS-R} had the largest global SHAP contribution, followed by \gls{FVC}, age, and respiratory rate; Logistic Regression gave a compatible overall ranking.
    Local SHAP and \gls{LIME} shared 50--70\% of their top-ten features across the four case studies, which supports partial agreement but not interchangeability.
    Error profiling also showed a relevant limitation: recall fell to 54.5\% in the mildest baseline \gls{ALSFRS-R} subgroup ($>42$), where five of the 12 false negatives occurred.

  \item \textbf{The supplementary analyses support further investigation, not clinical use.}
    Decision Curve Analysis estimated positive net benefit relative to both reference strategies across the main interval $t\in[0.18,\,0.47]$, while the learning curves did not show a clear performance plateau.
    Partial dependence patterns and the patient-level report provide additional views of model behaviour, but neither establishes causal effects, external validity, or readiness for individual clinical decisions.
\end{enumerate}

\bigskip
\noindent
Taken together, Study~I provides a reproducible framework for short-term functional progression prediction rather than a model ready for deployment.
Its main contribution lies in the complete evaluation protocol: temporally valid target construction, subject-wise validation, metric-led model selection, development-only threshold choice, held-out testing, and post-hoc explanation.
The distinction between ranking performance and threshold-dependent behaviour becomes particularly important in Study~II, where the class imbalance is more severe and the target changes from functional decline to survival.
That study is described in Chapter~\ref{ch:study2_methods}.
